<h1>Details page</h1>

<h2>Diary title</h2>
<p><%= @list.title %></p>

<h2>Diary Text</h2>
<p><%= @list.body %></p>

<h2>Image</h2>
  <%= image_tag @list.image, size: "300x300" %>

<%= link_to "Edit page", edit_list_path(@list.id) %>
<%= link_to "Delete", list_path(@list.id), method: :delete, "data-confirm" => "Do you really want to delete this?" %>